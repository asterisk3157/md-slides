# md-slides 今後の TODO

## アニメーション拡張

### 1. 見出し（ヘッダー）の既定フェード
- 各スライドの見出しブロックを、既定で **左→右フェードイン**（不透明度＋左からのスライド）で表示する。
- 現状は全ブロック共通の「左→右ワイプ（クリック送り）」。見出しはフェード系にして掴みを良くする。
- pptx: 見出しブロックに Fade/FlyIn(fromLeft) 系の entr アニメ。SVG プレビュー: CSS で軽くプレビュー。

### 2. 編集バーに方向アニメ（十字4方向の矢印）
- ✅ fmtbar に4方向矢印（←↑↓→）。ブロック選択中に押すと per-block の登場方向を
  override.anim に保存、pptx で `wipe(<dir>)` の方向別エントランスに反映。アクティブ表示あり。
- 🚧 残: 「手前（視点側）に浮かび上がる」奥行き感（zoom/grow を合成）。現状は方向ワイプのみ。
- 🚧 残: SVG プレビューでの再生（現状はボタンのアクティブ表示のみ）。
- 注: wipe の方向文字列は実機 PowerPoint で向きを要確認（左右上下が反転する場合あり）。
