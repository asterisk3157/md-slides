name = "md-slides"
compatibility_date = "2025-01-01"
pages_build_output_dir = "public"

# テキスト専用 md→slide。辞書・D1・サーバ生成なし＝原則 静的配信。
# 認証も不要（公開可）。Pages Functions は使っていない (handwriting 版の
# functions/api/{export,theme}.ts は削除済み)。
